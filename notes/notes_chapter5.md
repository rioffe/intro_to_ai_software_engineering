5.2.3

Maybe we should be more explicit? E.g. create tests directory: mkdir tests
Then vi tests/test_scratch.py and copy the lines below. Or is it understood by now?

Also, it says Run it. Should we be explicit how to run it, e.g. 

python tests/test_scratch.py  

But the above would not run, because it is just a function and we haven't introduced pytest yet.:
